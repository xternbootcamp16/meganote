{
  "name": "MeganoteSeed",
  "private": true,
  "version": "0.5.0",
  "description": "A starter project for an AngularJS 1.x note-taking app.",
  "repository": "git@github.com/xternbootcamp16/meganote-seed.git",
  "license": "MIT",
  "devDependencies": {
    "bower": "^1.7.9",
    "http-server": "^0.9.0"
  },
  "scripts": {
    "start": "http-server app -a localhost -p 8000 -c-1",
    "prestart": "npm install",
    "postinstall": "bower install",
    "preupdate-webdriver": "npm install"
  }
}
